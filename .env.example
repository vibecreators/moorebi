# MOORE OS — environment. Copy to .env.local. Never commit real secrets.
#
# Every connection below is carried over from the predecessor build
# (docs/15-decision-log.md D-002) — no new credentials were issued.

# --- Supabase (project: moore-os / cktzzimjziuantrgemxe) ---
NEXT_PUBLIC_SUPABASE_URL=https://cktzzimjziuantrgemxe.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable__jAUTUrBMjTp2Fi-9mfZdQ_YavxxAWk
# Server-only. Bypasses RLS — never expose to the client, never inline in a page.
SUPABASE_SERVICE_ROLE_KEY=__set_me__

# --- PostHog (project 198696, EU) ---
# Same project as the predecessor, so history is continuous. New-build events
# are namespaced `mos.*` so the two products stay separable in analysis.
NEXT_PUBLIC_POSTHOG_KEY=phc_no6U63rTp3aT5Wvkpd7TGU844yPX8Ambcc3zrGHwTXDy
NEXT_PUBLIC_POSTHOG_HOST=https://eu.posthog.com

# --- Anthropic (Copilot; Phase 1 uses it only for the briefing summary) ---
ANTHROPIC_API_KEY=

# --- n8n webhook (reminder fan-out). Unset = the app no-ops the fan-out. ---
N8N_WEBHOOK_URL=

# --- Tests ---
# Password used for the disposable tenants created by tests/isolation.test.ts.
# REQUIRED in CI: the isolation suite fails rather than skips without it.
TEST_USER_PASSWORD=
